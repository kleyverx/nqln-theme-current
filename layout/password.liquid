<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    {% render 'global-tokens' %}
    {{ 'base.css' | asset_url | stylesheet_tag }}

    {% # Social, title, etc. %}
    {% render 'meta-tags' %}

    {{ content_for_header }}
  </head>

  <body>
    {{ content_for_layout }}
  </body>
</html>
